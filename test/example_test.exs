defmodule ExampleTest do
  @moduledoc false
  
  use ExUnit.Case

  alias Example

  describe "Example" do
    test "write some tests" do
      flunk("no tests for #{Example} yet!")
    end
  end
end
